export default async function handler(req,res){if(req.method!=='POST'){res.setHeader('Allow','POST');return res.status(405).json({error:'Method not allowed.'});}const ct=String(req.headers['content-type']||'');if(!ct.includes('application/json'))return res.status(415).json({error:'JSON required.'});const raw=JSON.stringify(req.body||{});if(Buffer.byteLength(raw)>32768)return res.status(413).json({error:'Request too large.'});try{const up=await fetch('https://aiteitai-growth.187-127-167-128.sslip.io:8443/website-preview/api/project-inquiries',{method:'POST',headers:{'content-type':'application/json','user-agent':'AITEIT-Vercel-Proxy/1.0'},body:raw});const text=await up.text();res.status(up.status);res.setHeader('content-type',up.headers.get('content-type')||'application/json');return res.send(text);}catch(e){return res.status(502).json({error:'Inquiry service temporarily unavailable.'});}}
